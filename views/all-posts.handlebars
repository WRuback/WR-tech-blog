{{#each posts as |post|}}
<div class="card">
    <div class="card-header flex-row justify-space-between align-center">
        <h2>{{title}}</h2>
        <p>Posted by {{username}} on {{format_date created_at}}</p>
    </div>
    <P class="card-body">{{body}}</P>
</div>
{{/each}}